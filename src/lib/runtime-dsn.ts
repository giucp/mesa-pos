/** Optional deploy overlay. Keep null in git. Production copy is injected at install. */
export const runtimeDatabaseUrl: string | null = null;
export const runtimeDirectUrl: string | null = null;
export const runtimeSessionSecret: string | null = null;
